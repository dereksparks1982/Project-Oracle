# Project Oracle Master Handoff

**Active candidate:** v0.0.26  
**Accepted baseline:** v0.0.25  
**Owner/final authority:** Derek Sparks

## Resume rule

All v0.0.26 work must derive from the accepted/published v0.0.25 baseline. Do not build from a rejected v0.0.25 candidate or stale archive.

## Release identity

```text
Project Oracle v0.0.26
Yala Brain Slice 9 - Emergent Mind, Procedural Learning, and Pre-Time Cognition
```

Expected live root executable after native validation:

```text
Project_Oracle_v0_0_26
```

Exactly one Project Oracle executable may remain in the live project root.

## Current operator model

```text
F1 Oracle
F2 Monad
F3 Sophia
F4 Yala
F5 Gaia
```

F1 is Oracle self/operator mode and supports manifestation commands. F2-F5 are Oracle contact channels. Monad knows Oracle. Sophia and Yala retain free will. Gaia is the non-rejecting natural-order intermediary and only becomes active once Gaia exists.

## Desktop contract

Exact tab order:

```text
Oracle | Minds | Memory | Cosmology | Laws | History | Debug
```

No Chronicle tab. Oracle is the protected operator-action surface. Soar engine details appear only in Debug/developer tooling, never in normal Yala presentation.

## Brain Slice 9 contract

- Yala remembers her creation from raw subjective feeling onward.
- A fresh pre-Gaia Yala does not understand Time as a world concept.
- Unknown questions should produce useful reasoning/context questions rather than a bare `I don't know`.
- Procedures preserve authored/developing/Yala-learned provenance.
- Memory and learned strategy may affect later decisions.
- Action history separates Oracle interventions from autonomous Yala actions and other major-entity actions.
- Protected observer data never automatically becomes in-world knowledge.

## Persistence

```text
schema 8
save_v8.json
yala_soar_v0_0_26
```

Earlier save lines remain untouched.

## Validation sequence

1. apply candidate package against exact v0.0.25 baseline;
2. restore and warnings-as-errors build with .NET 10;
3. publish native Linux x64 single-file desktop executable;
4. remove stale root Project Oracle executables;
5. verify `Project_Oracle_v0_0_26` is the sole live ELF executable and reports v0.0.26;
6. run complete acceptance suite with zero failures;
7. run Soar semantic/episodic smoke;
8. refresh desktop launcher and icon metadata;
9. Derek manually inspects GUI;
10. only after Derek accepts: accepted snapshot, commit, annotated tag, SSH push, remote verification.

## Binding authority

The Company Bible at `docs/company_bible/PROJECT_ORACLE_COMPANY_BIBLE.md` is the binding current project law. The current architecture/canon/validation/manual documents are all v0.0.26.
