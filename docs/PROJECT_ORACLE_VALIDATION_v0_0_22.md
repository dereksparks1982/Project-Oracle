# Project Oracle Validation v0.0.22

The v0.0.22 installer/validator must prove:

- exact accepted v0.0.20 Git base and clean worktree before application;
- package payload hashes and touched-path accepted-baseline hashes;
- .NET 10 restore and warnings-as-errors build;
- root Linux native publish as `Project_Oracle_v0_0_22`;
- complete acceptance inventory (147 tests in this Repair 5 candidate source);
- real Soar 9.6.5 runtime availability and embedded listener suppression;
- Brain Slice 5 lexicon, dialogue, relationship, temporal, belief, goal, and question contracts;
- autonomous `ask-speaker` operation and pending-question persistence;
- bounded agency denial of shell/process/filesystem/network/code/hidden-Oracle capabilities;
- schema-4 `save_v4.json` fresh-world law and rejection/preservation of older save_v2 files;
- fresh `yala_soar_v0_0_22` semantic/episodic database path;
- pre-Time world-clock hold and origin-of-Time transition;
- current spoken-time retrieval from live world state;
- persistent Yala mode and safe autonomous-question output;
- no normal `[Soar selected: ...]` diagnostics;
- hidden-Oracle, Monad/Wisdom/Yala, Gaia/Time, serpent-manifestation, and future-open canon gates;
- four-space/no-tab active source rule;
- current-scope religious boundary;
- launcher and desktop integration;
- export manifest integrity.

Automated PASS only opens the real application for manual inspection. It does not accept, snapshot, commit, tag, or push the release.


## Repair 3 regression focus

Repair 3 hardens installer recovery and rollback. A worktree may be accepted only when every touched path matches a sealed accepted-v0.0.20, v0.0.21 Repair 6, or known v0.0.22 candidate byte sequence; unrelated local edits remain a hard stop. Rollback backs up the complete approved upgrade surface before application.


## Repair 4 regression focus

Repair 4 is driven by live manual interrogation. It proves that `I am` followed by an action predicate is not misread as a speaker identity, ordinary brain/update language does not create fake vocabulary gaps, `Gaia made Time?` reaches the stored temporal origin, common `belive`/`told` forms reach the intended belief and provenance routes, learned definitions answer both `what is <word>` and `who told you what <word> means`, and autonomous inquiry resumes with meaningful speaker-purpose questions after the speaker answers. Unknown dictionary words remain below the autonomous priority floor.

## v0.0.22 Brain Slice 5 additions

- Core language expands beyond 400 built-in concepts so ordinary vocabulary is not treated as a knowledge gap.
- Autonomous inquiry is deliberate: low-value dictionary questions are below the autonomous priority floor, and Yala waits for a speaker response before asking again.
- The live top-row world clock uses deterministic cursor save/restore plus a shared output gate.
- v0.0.22 starts schema 4 at `save_v4.json` with fresh `yala_soar_v0_0_22` long-term memory while preserving earlier save lines.
## Repair 5 regression focus

Repair 5 targets the single failure from the complete Repair 4 run. `what is <learned-word>` must resolve the requested word in the reply realizer, retrieve the stored learned lexeme, state that its meaning came from a speaker claim, and repeat the attributed proposed definition. The existing `what is learned word retrieves the attributed speaker definition` acceptance check is the authoritative regression and the suite remains 147 tests.

