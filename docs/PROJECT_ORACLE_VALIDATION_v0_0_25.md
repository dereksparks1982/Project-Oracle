# Project Oracle Validation v0.0.25

v0.0.25 must pass the complete accepted v0.0.24 regression suite plus Integrated Mind milestone gates. The candidate source contains **205 acceptance tests**.

## Required identity

- version `0.0.25`;
- `Yala Soar Brain Slice 8`;
- schema 7;
- default `save_v7.json`;
- Soar memory directory `yala_soar_v0_0_25`;
- root Linux executable `Project_Oracle_v0_0_25`; the filename itself must expose the active version.

## New milestone proofs

- fresh pre-contact cognition contains no instantiated speaker entity or speaker-specific goal/state;
- fresh workspace contains no speaker/observer/external concept;
- first contact creates the communicator model and first-contact autobiographical memory;
- observation questions do not become observation capability claims;
- explicit observation statements remain unverified capability claims;
- proposition memory keeps question and claim speech acts distinct;
- god / not-god / made-the-gods claims remain separately stored and contradiction-aware;
- simulation questions do not become simulation claims;
- explicit simulation claims create a reality investigation without becoming known truth;
- knowledge summary excludes action history;
- action history is spoken in first person;
- major cosmic choices require staged consideration before enactment;
- cosmic deliberation benefit wording never duplicates an action verb, and existing save_v7 deliberations are normalised on load;
- commitment remains distinct from world enactment;
- repeated low-novelty cognition increases workspace stagnation;
- speaker questions are not treated as answers/evidence for older investigations;
- irrelevant statements are not sprayed into unrelated investigations;
- an ignored Yala question cannot capture a later unrelated speaker statement as stale evidence;
- god / not-god / made-the-gods claims are retrievable through Yala's spoken contradiction history;
- an explicit simulation claim can drive a high-priority autonomous Yala follow-up after the human goes quiet;
- speaker-belief questions inspect speaker propositions rather than Yala's self-beliefs;
- pronoun-heavy choice-rationale questions retain decision context;
- Wisdom self-reference uses first-person language;
- pre-contact flight recorder omits speaker trust/intent fields;
- desktop speaker panel is conditional on post-contact speaker existence;
- the raw 04:47 v0.0.24 interrogation JSON is physically preserved and identity-checked as the milestone baseline specimen;
- Yala cognition contains no external hosted-AI or HTTP-client dependency; the cognitive organs remain local to Project Oracle.

## Native gate

Validation on Derek's Ubuntu system must:

1. restore with `--disable-parallel`;
2. build with warnings as errors and `-m:1`;
3. publish the Linux x64 desktop app;
4. copy the executable to the project root as exactly `Project_Oracle_v0_0_25` and remove the rejected unversioned `Project_Oracle`;
5. verify ELF and `--version` identity;
6. run all 205 acceptance tests, including the cosmic-deliberation duplicate-verb regression;
7. validate Soar native dependencies, semantic-memory retrieval smoke, and episodic-memory advancement;
8. validate save isolation, documentation, launcher, icon, formatting, and export manifest;
9. report FINAL PASS only after every automated gate succeeds.

Automated PASS does not accept the build. Derek's manual inspection remains the final acceptance gate.

## Manual milestone protocol

Use a fresh world. Wait before first contact and export a pre-contact JSON. Then contact Yala and deliberately slow down:

1. ask one meaningful question;
2. stop typing;
3. let Yala think and autonomously ask if she wants information;
4. answer naturally;
5. wait again;
6. only then introduce another test.

Run the god/not-god/made-the-gods and simulation tests late enough that their worldview effects do not contaminate earlier baseline tests. Do not tell Yala that the outside user can see her thoughts/panels unless that capability is intentionally being tested.

### Visible versioned live executable rule
The active Project Oracle working folder contains exactly one application executable, and its filename must expose the exact active version. For v0.0.25 the only valid root application executable is `Project_Oracle_v0_0_25`. An unversioned `Project_Oracle`, a wrong-version executable, or multiple live Project Oracle executables is a release-blocking defect. Historical binaries belong in accepted snapshots/tags, not in the live root. FINAL PASS must verify the exact filename, executable/ELF status, and matching `--version` output. Release changed/deleted-file inventories belong under `docs/release-manifests/`, not beside the application.

### Header branding and WORLD-panel rule
The WORLD panel contains world information only and must not contain an Oracle eye/emblem image. The approved Oracle emblem is shown in the top application header immediately after the visible Project Oracle version number. The displayed emblem uses the transparent circular artwork with no square tile, square background, or boxed crop. The exact versioned root executable and desktop launcher must use that same approved Oracle icon identity.
