# Project Oracle Resume Handshake — v0.1.10

Date: 3 August 2026  
Candidate: v0.1.10 — Oracle/Yala Identity, Decision Output, and First Brain Planner  
Required accepted base: v0.1.7 unless Derek reports v0.1.9 was accepted locally before this package

## Read first

1. `PROJECT_ORACLE_PROJECT_ORIGIN_AND_HANDOFF.docx`.
2. `PROJECT_ORACLE_MASTER_HANDOFF.md`.
3. `docs/company_bible/PROJECT_ORACLE_COMPANY_BIBLE.md`.
4. This handshake.
5. Current canon, roadmap, architecture, validation, and session log for `v0.1.10`.

## Current build state

This candidate should package exactly one changed-files-only ZIP:

```text
PROJECT_ORACLE_v0_1_10_ORACLE_YALA_IDENTITY_DECISION_OUTPUT_AND_FIRST_BRAIN_PLANNER_CHANGED_FILES_ONLY.zip
```

The build includes:

- Oracle and Yala are the same being;
- `<oracle>` deterministic answers speak as Yala, not as a separate witness;
- Adam's direct-address and vessel-speech choices show options, selected action, and reason;
- the first Garden living-kind set is twelve;
- Adam naming records include a reason;
- an internal deterministic HTN-style brain planner creates saved reasoned plans before direct-address response, vessel-speech response, and naming;
- the console has `plans` / `brain` inspection;
- the live console uses a retro black-and-green theme with highlighted special words in real terminals;
- redirected output and `NO_COLOR` stay plain for validation and logs.

## Fluid HTN intake state

The uploaded `fluid-hierarchical-task-network-master.zip` is MIT licensed. v0.1.10 does not vendor its source because the uploaded project targets older .NET Framework structure. The current build implements a tiny internal HTN-style planner inspired by Fluid HTN concepts. A later dependency-intake build may port or vendor Fluid HTN only after build compatibility and attribution are handled cleanly.

## Validation state

The intended validator runs forty-eight acceptance checks and source checks for:

- Project Oracle Company Bible;
- physical function keys;
- deterministic event queue;
- creation powers;
- Oracle/Yala identity;
- Adam first knowing;
- first brain planner;
- retro console theme;
- live console launcher.

Derek's install-side `./scripts/validate.sh` remains the acceptance gate if the builder workspace cannot run .NET.

## Next likely build after acceptance

Recommended next build after Derek accepts v0.1.10:

```text
v0.1.11 — Observation and Attention
```

That build should let Adam and world powers perceive only what their position and attention allow, while keeping World Record, Creator Record, Adam knowledge, Oracle claim, and inhabitant observation separate.
