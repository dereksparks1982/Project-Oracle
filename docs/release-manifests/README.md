# Project Oracle release manifests

Changed-file and deleted-file inventories belong here, not in the live project root.

The active Project Oracle root is reserved for the exact versioned application executable and core project files. The executable filename must expose the active version (for v0.0.25: `Project_Oracle_v0_0_25`). Historical release inventories are documentation, while accepted binary history belongs in accepted snapshots and Git tags.
